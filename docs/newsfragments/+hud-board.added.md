`vissue hud` opens on the project list. Opening a project shows that
project's ready forest, then List / Claims / Agenda / Search inside it.
The iced chrome uses the seat rofi palette (mocha, blue selected row)
and a 16px base text size so unsized widgets do not inherit the title
size. `--rofi` keeps the seat dmenu picker.
