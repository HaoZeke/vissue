The HUD keeps a search field with the search icon on the action row.
Inside a project it finds that project's issues and nests them under
their parents. On the project list it searches the vault. `/` focuses
it. The Search chip is gone; Tab cycles Ready / List / Claims / Agenda.
