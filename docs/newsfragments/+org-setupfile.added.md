`#+SETUPFILE:` (local files only) merges TODO keywords, tags, and
`#+PRIORITIES:`. A missing priority cookie uses the file default.
`:ORDERED:` holds later siblings in `ready`; `:NOBLOCKING:` skips
that wait. An org-gcal `:ID:` with a slash is not an issue id.
